{
  "name": "component-builder",
  "main": "./index.mjs",
  "scripts": {
    "ci": "npm i",
    "build": "npx vite build",
    "bundle": "tar -czf ./vue-mini.tgz --exclude vue-mini.tgz --exclude node_modules/ --exclude src/ ."
  },
  "dependencies": {
    "@vitejs/plugin-vue": "^5.0.5",
    "vite": "^5.3.1",
    "vue": "^3.4.31",
    "@cloud-cli/http": "latest"
  }
}
